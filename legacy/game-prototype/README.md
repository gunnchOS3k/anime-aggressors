# Legacy Game Prototype (`legacy/game-prototype`)

**Status:** Archived experimental TypeScript/C++ prototype — not part of CI.

Contains early `AnimeAggressorsGame.ts`, fighting systems, and `performance_engine.cpp` that predates `packages/game-core`.

Active deterministic simulation: **`packages/game-core`**.

Native engine expansion track: **`native/engine/`** (see `docs/CPP_ENGINE_PLAN.md`).
