{
  "name": "anime-aggressors-web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anime-aggressors/edgeio": "1.0.0",
    "@anime-aggressors/game-core": "0.1.0",
    "@anime-aggressors/rollback": "0.1.0",
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
