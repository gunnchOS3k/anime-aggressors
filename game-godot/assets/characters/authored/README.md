# Authored runtime imports

These GLBs are Blender → glTF 2.0 exports for the authored pipeline proof clip `pipeline_proof`.

They are AUTHORED_WIP pose-blocks, not final animation and not final character art.
Procedural `.anim.json` clips remain the fallback hero set.
