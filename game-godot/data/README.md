# data/

| Folder | Contents |
|--------|----------|
| `fighters/` | Normalized roster JSON + animation manifests |
| `moves/` | Per-fighter move timelines + `move_schema.json` |
| `stages/` | Production stage layouts |
| `rulesets/` | Stock/time rules |
| `balance/` | Global constants (`base_constants.json`) |

Regenerate: `npm run generate:godot-full-scope` from repo root.
