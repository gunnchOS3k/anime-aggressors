# Blender Sources

Place fighter `.blend` files under:

```
fighters/<fighter-id>/<fighter-id>.blend
stages/<stage-id>/
props/
vfx/
```

Export to `../exports/godot/` and `../exports/unreal/` using naming in `docs/fighters/*_PRODUCTION_SPEC.md`.
