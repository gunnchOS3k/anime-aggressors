# Ember Vale — authored animation

Lane: Flame

Source `.blend` lives in `source/` (Git LFS when configured).
Exports live in `export/`. Human candidates stage at
`game-godot/content/human_art_staging/ember-vale/`.

Wave A 14 hero actions are defined in the roster manifest. They are not complete.
