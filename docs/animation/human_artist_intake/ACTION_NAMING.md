# Action naming

Wave A hero actions (14 × 7 = 98), defined not complete:

`idle walk run dash jump light medium heavy aura super hurt_heavy launch charge ko`

Golden Slice extras that may appear: `charged_idle`, `clash_lock`.

Clip name in the GLB **must** match the action id. Gameplay frame data stays on existing move JSON.
If acting is late, say so in the sidecar. Do not ask engineering to move the hitbox.
