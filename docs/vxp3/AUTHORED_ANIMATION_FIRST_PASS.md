# VXP-3 Authored Animation — First Pass (§29)

Draft PR #106. Do not merge. RC1 `v1.0.0-rc.1` untouched.

This pass moves animation-content authority to:

`artist-authored source → export → Godot import → runtime mapping`

Procedural `.anim.json` clips remain fallback / blockout / regression fixtures. They are **not** final authored animation.

Human and merge gates stay false.
