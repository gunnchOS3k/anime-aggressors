# Local visible milestone — Anime (2026-07-15)

## Gate results (headless)

`tests/accept_visible_match.gd` completed with `RESULT failed=0` (combat/system assertions).

Screenshots **could not** be captured under headless Dummy renderer (`texture_2d_get` null). Pixel still required for image/video evidence.

## What is now visible in running menus (code)

| Feature | Status |
| --- | --- |
| Distinct silhouette cards on fighter select | YES — vector shapes per fighter |
| Model3D focus/lock-in on select | YES — wired |
| Personality / power / line text | YES |
| Living title fighter rotation | YES — boot cameo + BG tint |
| Per-fighter victory/defeat poses | YES — distinct tweens |
| Proxy GLB battle presentation | YES (still proxy anatomy) |

## Still not PASS

- Unique final character meshes
- Pixel capture package (§28)
- Signed APK from this branch
