# Wave017 Before / After — Debug / proxy labels

## Before

- `PROCEDURAL PRODUCTION PROXY` / `STYLIZED FALLBACK` / `PROXY — NOT FINAL ART` visible in player-facing battle.

## After

- ModelTierLabel and animator proxy label hidden unless developer/training debug HUD path.
- Harness: `PLAYER_BUILD_VISIBLE_DEBUG_LABELS=0` (`artifacts/wave017/PLAYER_BUILD_DEBUG_LABELS.json`)
